{
    "checks": {
    "<%= name %>_check": {
    "type": "metric",
		"handlers": ["<%= handler_name %>"],
		"command": "<%= check_command %>",
				"interval": <%= check_interval %>,
				"subscribers": [ <%= subscription_channels %> ]
      }
    }
}
